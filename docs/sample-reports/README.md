# Sample reports

These files are **illustrative** StockShield AI v1.0 exports (JSON, CSV, PDF). They use a frozen sample payload so the repository does not depend on live Yahoo quotes.

Generate your own from the CLI:

```bash
python app.py
```

Reports written at runtime land in `reports/` (gitignored).
